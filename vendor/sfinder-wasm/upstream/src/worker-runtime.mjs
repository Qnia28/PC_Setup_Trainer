import{loadWasmAssets,WasmPcSolver}from'./wasm-backend.mjs';import{calculateChance,calculateSaves,calculateMinimalsFeature,calculateFifthFeature,calculatePerSaveMinimalsFeature}from'./features.mjs';import{resolvePerSaveTargetLines}from'./per-save-minimals.mjs';import{calculateFourthDistribution}from'./fourth.mjs';
let assetsPromise;const solvers=new Map();async function getSolver(h){let s=solvers.get(h);if(s)return s;assetsPromise??=loadWasmAssets();const a=await assetsPromise;s=new WasmPcSolver(a.exports,h,h===4?a.legal:null);solvers.set(h,s);return s}
export async function runWorkerRequest(r){const clear=r.kind==='per-save-minimals'?resolvePerSaveTargetLines(r.input):(r.input?.clear??4),solver=await getSolver(clear),input={...r.input,solver};switch(r.kind){case'chance':return calculateChance(input);case'saves':return calculateSaves(input);case'minimals':return calculateMinimalsFeature(input);case'fourth':return calculateFourthDistribution(input);case'fifth':return calculateFifthFeature(input);case'per-save-minimals':return calculatePerSaveMinimalsFeature(input);default:throw new Error(`unknown request ${r.kind}`)}}
